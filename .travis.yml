language: generic
sudo: required
services:
  - docker


before_install:
  - docker build -t andrejpopov/docker-react -f Dockerfile.dev .

script:
  - docker run andrejpopov/docker-react npm run test -- --coverage